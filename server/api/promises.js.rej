diff a/server/api/promises.js b/server/api/promises.js	(rejected hunks)
@@ -1,3 +1,3 @@
-import { Router as router} from "express";
-import { db } from "../firebase";
-import { ref, get, set, update, remove } from 'firebase/database';
+import {Router as router} from 'express';
+import {db} from '../firebase';
+import {ref, get, set, update, remove} from 'firebase/database';
@@ -16 +16 @@ router.get('/:username', async (req, res, next) => {
-      console.log("No data available");
+      console.log('No data available');
@@ -26 +26,4 @@ router.get('/:username/:id', async (req, res, next) => {
-    const itemRef = await ref(db, `/promises/${req.params.username}/${req.params.id}`);
+    const itemRef = await ref(
+      db,
+      `/promises/${req.params.username}/${req.params.id}`,
+    );
@@ -34 +37 @@ router.get('/:username/:id', async (req, res, next) => {
-      console.log("No data available");
+      console.log('No data available');
@@ -57 +60 @@ router.post('/:username/:id', async (req, res, next) => {
-  const body = { item, url, instructions, promised: true, for: giftReceiver };
+  const body = {item, url, instructions, promised: true, for: giftReceiver};
@@ -64 +67 @@ router.post('/:username/:id', async (req, res, next) => {
-    await update(ref(db, `/wishlist/${giftReceiver}/${id}`), { promised: true });
+    await update(ref(db, `/wishlist/${giftReceiver}/${id}`), {promised: true});
@@ -73 +76 @@ router.post('/:username/:id', async (req, res, next) => {
-      res.status(404).json({ message: 'Wishlist not found' });
+      res.status(404).json({message: 'Wishlist not found'});
@@ -90 +93 @@ router.delete('/:username/:giftReceiver/:id', async (req, res, next) => {
-    await update(wishlistItemRef, { promised: false });
+    await update(wishlistItemRef, {promised: false});
