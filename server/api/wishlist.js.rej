diff a/server/api/wishlist.js b/server/api/wishlist.js	(rejected hunks)
@@ -1,3 +1,3 @@
-import { Router as router} from "express";
-import { db } from "../firebase";
-import { ref, get, push, update, remove } from 'firebase/database';
+import {Router as router} from 'express';
+import {db} from '../firebase';
+import {ref, get, push, update, remove} from 'firebase/database';
@@ -16 +16 @@ router.get('/:username', async (req, res, next) => {
-      console.log("No data available for the specified user");
+      console.log('No data available for the specified user');
@@ -26 +26,4 @@ router.get('/:username/:id', async (req, res, next) => {
-    const itemRef = ref(db, `/wishlist/${req.params.username}/${req.params.id}`);
+    const itemRef = ref(
+      db,
+      `/wishlist/${req.params.username}/${req.params.id}`,
+    );
@@ -34 +37 @@ router.get('/:username/:id', async (req, res, next) => {
-      console.log("No data available for the specified user");
+      console.log('No data available for the specified user');
@@ -46 +49 @@ router.post('/:username', async (req, res, next) => {
-  const body = { item, url, instructions, promised: false };
+  const body = {item, url, instructions, promised: false};
@@ -62 +65 @@ router.post('/:username', async (req, res, next) => {
-      res.status(404).json({ message: 'Wishlist not found' });
+      res.status(404).json({message: 'Wishlist not found'});
@@ -73 +76 @@ router.put('/:username/:id', async (req, res, next) => {
-  const body = { item, url, instructions, promised: false };
+  const body = {item, url, instructions, promised: false};
@@ -92 +95 @@ router.put('/:username/:id', async (req, res, next) => {
-      res.status(404).json({ message: 'Wishlist not found' });
+      res.status(404).json({message: 'Wishlist not found'});
@@ -94 +97 @@ router.put('/:username/:id', async (req, res, next) => {
-  }  catch (err) {
+  } catch (err) {
@@ -119 +122 @@ router.delete('/:username/:id', async (req, res, next) => {
-    res.json({ removedItem, message: `Item ${id} removed successfully` });
+    res.json({removedItem, message: `Item ${id} removed successfully`});
