diff a/server/auth/index.js b/server/auth/index.js	(rejected hunks)
@@ -1,4 +1,9 @@
-import { Router as router} from "express";
-import { auth, db } from "../firebase.js";
-import { ref, get } from 'firebase/database';
-import { createUserWithEmailAndPassword, signInWithEmailAndPassword, signOut, updateProfile } from 'firebase/auth';
+import {Router as router} from 'express';
+import {auth, db} from '../firebase.js';
+import {ref, get} from 'firebase/database';
+import {
+  createUserWithEmailAndPassword,
+  signInWithEmailAndPassword,
+  signOut,
+  updateProfile,
+} from 'firebase/auth';
@@ -20 +25,2 @@ router.post('/login', async (req, res, next) => {
-      }).then((userCredential) => {
+      })
+      .then((userCredential) => {
@@ -23 +29 @@ router.post('/login', async (req, res, next) => {
-        res.send(auth.currentUser)
+        res.send(auth.currentUser);
@@ -67 +73,2 @@ router.post('/signup', async (req, res, next) => {
-        }).then(async (userCredential) => {
+        })
+        .then(async (userCredential) => {
@@ -74 +81 @@ router.post('/signup', async (req, res, next) => {
-          await userTable.set({ ...allUsers, [username]: username });
+          await userTable.set({...allUsers, [username]: username});
