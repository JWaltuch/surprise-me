diff a/server/index.js b/server/index.js	(rejected hunks)
@@ -27 +27 @@ const createApp = () => {
-  app.use(express.urlencoded({ extended: true }));
+  app.use(express.urlencoded({extended: true}));
