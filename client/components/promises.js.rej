diff a/client/components/promises.js b/client/components/promises.js	(rejected hunks)
@@ -1,3 +1,3 @@
-import React, { Component } from 'react';
-import { PromisesItem } from './promises-item';
-import { withRouter } from 'react-router-dom';
+import React, {Component} from 'react';
+import {PromisesItem} from './promises-item';
+import {withRouter} from 'react-router-dom';
@@ -5 +5 @@ import axios from 'axios';
-import { getDatabase, ref } from 'firebase/database';
+import {getDatabase, ref} from 'firebase/database';
@@ -12 +12 @@ export default withRouter(
-      this.state = { promises: {} };
+      this.state = {promises: {}};
@@ -18,4 +18,2 @@ export default withRouter(
-      const { data } = await axios.get(
-        `/api/promises/${this.props.currentUser}`,
-      );
-      this.setState({ promises: data });
+      const {data} = await axios.get(`/api/promises/${this.props.currentUser}`);
+      this.setState({promises: data});
@@ -24 +22 @@ export default withRouter(
-        this.setState({ promises: snapshot.val() });
+        this.setState({promises: snapshot.val()});
